// 참 또는 거짓 (true, false) -> 상반된 둘 중 하나의 값을 담을 수 있음
// 직접 할당되기 보다는 반환값으로 프로그램 곳곳에서 활용
let isEmployed = true;
let isMarried = false;

console.log("직업 유무 : ", isEmployed);
console.log("기혼 여부 : ", isMarried);

const a = 1 > 2;
const b = 1 < 2;

console.log(a, typeof a);
console.log(b, typeof b);