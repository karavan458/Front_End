// 숫자 number 
// - 자바스크립트에는 정수와 실수의 구분이 없음(정수도 실수로 처리)
// - 정수는 2^53 - 1 까지는 안정적으로 표현가능 (이보다 더 큰 정수는 BigInt)

let integer = 100;
let real = 12.34;
let negative = -99;

console.log(integer, real, negative);